[workspace]
members = [
    "programs/agentkeys",
    "programs/bonding_curve",
    "programs/treasury", 
    "programs/access_control"
]

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1